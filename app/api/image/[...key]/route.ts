import { env } from "cloudflare:workers";
export async function GET(request:Request,{params}:{params:Promise<{key:string[]}>}){const cookie=request.headers.get("cookie")?.match(/(?:^|;\s*)fivek_session=([^;]+)/);if(!cookie)return new Response("Unauthorized",{status:401});const member=await env.DB.prepare("SELECT m.id,m.role FROM sessions s JOIN members m ON m.id=s.member_id WHERE s.token=? AND s.expires_at>? AND m.active=1").bind(cookie[1],new Date().toISOString()).first<any>();if(!member)return new Response("Unauthorized",{status:401});const key=(await params).key.join("/");let allowed=member.role==="admin";if(!allowed&&key.startsWith("airdrop/"))allowed=!!await env.DB.prepare("SELECT id FROM airdrop_submissions WHERE image_key=? AND member_id=?").bind(key,member.id).first();if(!allowed&&key.startsWith("party/"))allowed=!!await env.DB.prepare("SELECT pa.id FROM party_activities pa JOIN party_activity_members pam ON pam.party_activity_id=pa.id WHERE pa.image_key=? AND pam.member_id=?").bind(key,member.id).first();if(!allowed)return new Response("Forbidden",{status:403});const object=await env.BUCKET.get(key);if(!object)return new Response("Not found",{status:404});return new Response(object.body,{headers:{"content-type":object.httpMetadata?.contentType||"image/jpeg","cache-control":"private, max-age=3600"}});}

